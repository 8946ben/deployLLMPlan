# AGENTS.md — 多会话协作规则

本项目可能被**多个 ZCode 会话同时修改**,且允许出现不同会话改同一个文件的情况。为保证互斥与一致,任何会话在编辑文件前必须遵守以下流程。这些规则对 AI 会话是强约定:除非用户明确要求,不得绕过。

## 写锁流程(编辑任何文件之前)

1. **确定范围**:想清楚本次任务要改哪些文件,写成 scope(文件名如 `engine.js`,目录如 `tests/`,整库用 `*`)。
2. **取锁**:`node tools/lock.js acquire --id <会话标识> --scope <范围> --note "<一句话任务>"`
   - 失败 = 范围与在座会话重叠:进入**只读模式**,可以看代码、回答问题、跑测试,但禁止编辑;每隔几分钟用 `node tools/lock.js status` 重试取锁。
   - 成功 = 获得写租约(10 分钟)。长任务每 5 分钟 `node tools/lock.js renew --id <id>` 续期。
3. **改前重读**:所有要编辑的文件先重新 Read——上一任持有者可能刚改过内容,凭旧记忆编辑会被工具拒绝或产生错误 diff。
4. **释放前两件事**:`node tests/run-tests.js` 全部通过 → `git commit`(提交信息写明改了什么)。然后 `node tools/lock.js release --id <id>`(工作区不干净时 release 会被拒绝,这是故意的)。
5. **接管**:发现某把锁超过 10 分钟未续期,说明原会话可能已崩溃,可重新 acquire 自动接管(会记入 `.session-locks/history.log`)。

## 附加约定

- scope 精确到本次真正要改的文件,不要随手写 `*`——范围不重叠的会话可以并行工作。
- 不改锁范围之外的文件;临时验证脚本用完即删,不要留在仓库根目录。
- `AGENTS.md`、`tools/lock.js` 本身视为公共基础设施,改动 scope 需包含 `*` 并在 note 里说明。
- 用户在前台驱动的会话优先级最高:用户明确要求"现在就改 X"时,可以先向持有者范围取锁,取不到就如实告知用户由其裁决,不要静默绕锁。

## 兜底机制(为什么这套是可靠的)

- **工具层**:ZCode 的 Edit 在文件被外部修改后会拒绝执行并要求重读,单方面覆盖他人改动会直接报错。
- **git 层**:取锁后每完成一个原子步骤就 commit。即使约定被破坏出现同时写入,后写者会覆盖先写者的**未提交**修改——所以频繁提交是最后防线,历史里永远找得回。
